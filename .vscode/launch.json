{
  "version": "2.0.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "localhost에 대해 Chrome 시작",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}/src"
    }
  ]
}
